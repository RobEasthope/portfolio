import styled from 'styled-components';

const PortfolioTitle = styled('div')`
  text-align: left;
  font-size: 5.25rem;
  font-weight: 500;
  line-height: 1.2em;
  width: 90%;
  padding: 2em 0 1em;
`;

export default PortfolioTitle;
