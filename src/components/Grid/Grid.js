import styled from 'styled-components';
import Grid from 'styled-components-grid';

export const Block = styled(Grid.Unit)`
  padding: 0 10px;
`;

export const Row = styled(Grid)`
  margin: 0 -10px;
`;
