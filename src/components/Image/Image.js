import styled from 'styled-components';

import ValidImage from '../ValidImage/ValidImage';
import rem from '../../utils/rem';

const Image = styled(ValidImage)`
  margin-bottom: ${rem(16 * 1.65)};
`;

export default Image;
